test_flowRep.search <- function() {
    ## TODO Uncomment these when API search pushed to production
    #checkTrue(length(flowRep.search("Spidlen")) >= 5)
    #checkTrue(length(flowRep.search("CD11b")) >= 1)   
}